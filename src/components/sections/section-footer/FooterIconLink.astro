---
interface Props {
  url?: string;
}

const { url } = Astro.props;
---

<a
  class="w-10 h-10 flex items-center justify-center rounded-full hover:scale-110 transition-[transform] border border-solid border-black-950"
  href={url}
  target="_blank"><slot /></a
>
