---
interface Props {
  href: string;
  target?: string;
  rel?: string;
}

const { href, target = "_self", rel } = Astro.props;
---
<a
  class="text-sm flex items-center gap-1 hover:scale-105 transition-transform"
  href={href}
  target={target}
  rel={rel}
>
  <slot />
</a>